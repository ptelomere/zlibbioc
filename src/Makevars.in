PKG_CFLAGS+=-DZ_PREFIX -include zlib-1.2.5/zconf.h -include zlib-1.2.5/zlib.h

all: $(SHLIB)

$(SHLIB): zlib

zlib:
	make -C zlib-1.2.5 -fMakefile clean libzbioc.a libzbioc.so.1.2.5
	cp zlib-1.2.5/libzbioc.so libzbioc${DYLIB_EXT}

clean:
	make -C zlib-1.2.5 -fMakefile clean
